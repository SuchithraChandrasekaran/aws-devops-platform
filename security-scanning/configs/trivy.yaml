# Trivy configuration for security scanning
severity:
  - CRITICAL
  - HIGH
  - MEDIUM

vulnerability:
  type:
    - os
    - library

scan:
  skip-dirs:
    - node_modules
    - vendor
    - .git

output:
  format: table
  
rego:
  skip-policy-update: false
